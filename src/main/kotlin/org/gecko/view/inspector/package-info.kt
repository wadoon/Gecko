/**
 * Groups classes which model and create [Inspectors][org.gecko.view.inspector.Inspector] and
 * inspector-specific elements. An inspector is a scrollbar that allows the property values of
 * elements to be modified in the view. The [org.gecko.view.inspector.InspectorFactory] and the
 * [org.gecko.view.inspector.InspectorFactoryVisitor] are responsible for creating different types
 * of inspectors depending on the element that the inspector is built for.
 */
package org.gecko.view.inspector
